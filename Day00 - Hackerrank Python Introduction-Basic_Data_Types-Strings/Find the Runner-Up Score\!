if __name__ == '__main__':
    n = int(input())
    arr = [int(x) for x in input().split()]
    maximum = max(arr)
    
    i = 0
    while (i < n):
        if maximum == max(arr):
            arr.remove(max(arr))
        i = i+1
        
print(max(arr))
